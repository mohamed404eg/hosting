'use strict';

/**
 * pending-requests-order controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pending-requests-order.pending-requests-order');
